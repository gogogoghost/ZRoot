package site.zbyte.root;

public class Config {
    /**
     * Empty when use default remote class
     * If use zRoot-builder, it will change to your custom class
     * Runner will construct a instance at start
     */
    public static String RemoteClass="";
}
